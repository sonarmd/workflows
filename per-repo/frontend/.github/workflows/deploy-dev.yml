name: Deploy — Dev

on:
  push:
    tags: ['dev-fe-*']

concurrency:
  group: deploy-dev-fe
  cancel-in-progress: false

env:
  NODE_OPTIONS: --openssl-legacy-provider
  AWS_ROLE_ARN: ${{ vars.AWS_DEPLOY_ROLE_ARN }}

permissions:
  id-token: write
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - app: admin
            bucket: admin.dev.sonarmd.com
          - app: patient
            bucket: my.dev.sonarmd.com
          - app: provider
            bucket: care.dev.sonarmd.com
          - app: seat
            bucket: seat.dev.sonarmd.com
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: sonarmd/workflows/actions/setup-node@main
        with:
          node-version: '18'

      - name: Build shared + app
        run: |
          yarn build-shared
          REACT_APP_VERSION=${GITHUB_REF_NAME} yarn build-${{ matrix.app }}-dev

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-${{ matrix.app }}-dev
          path: ${{ matrix.app }}/build/
          retention-days: 3

  deploy:
    needs: [build]
    strategy:
      matrix:
        include:
          - app: admin
            bucket: admin.dev.sonarmd.com
          - app: patient
            bucket: my.dev.sonarmd.com
          - app: provider
            bucket: care.dev.sonarmd.com
          - app: seat
            bucket: seat.dev.sonarmd.com
    uses: sonarmd/workflows/.github/workflows/deploy-s3-cloudfront.yml@main
    with:
      environment: dev
      app-name: ${{ matrix.app }}
      bucket: ${{ matrix.bucket }}
      build-artifact-name: build-${{ matrix.app }}-dev
      aws-region: us-east-2
      aws-role-arn: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
    secrets:
      OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}

  started:
    runs-on: ubuntu-latest
    outputs:
      timestamp: ${{ steps.ts.outputs.timestamp }}
    steps:
      - id: ts
        run: echo "timestamp=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_OUTPUT"

  notify:
    needs: [deploy, started]
    if: always()
    uses: sonarmd/workflows/.github/workflows/notify-slack.yml@main
    with:
      status: ${{ needs.deploy.result }}
      environment: dev
      app-name: frontend
      tag: ${{ github.ref_name }}
    secrets:
      OP_SERVICE_ACCOUNT_TOKEN: ${{ secrets.OP_SERVICE_ACCOUNT_TOKEN }}

  metrics:
    needs: [deploy, started]
    if: always()
    uses: sonarmd/workflows/.github/workflows/metrics-collector.yml@main
    with:
      repo: sonarmd/frontend
      environment: dev
      tag: ${{ github.ref_name }}
      status: ${{ needs.deploy.result }}
      started: ${{ needs.started.outputs.timestamp }}
      aws-role-arn: ${{ vars.AWS_DEPLOY_ROLE_ARN }}
    permissions:
      id-token: write
      contents: read
