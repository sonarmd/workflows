name: CI

on:
  push:
    branches: ['**']
  pull_request:
    branches: [staging, master]

permissions: {}

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_OPTIONS: --openssl-legacy-provider

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      admin: ${{ steps.filter.outputs.admin }}
      patient: ${{ steps.filter.outputs.patient }}
      provider: ${{ steps.filter.outputs.provider }}
      seat: ${{ steps.filter.outputs.seat }}
      shared: ${{ steps.filter.outputs.shared }}
      any: ${{ steps.filter.outputs.any }}
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - uses: sonarmd/workflows/actions/detect-changed-apps@main
        id: filter

  deps:
    needs: [detect-changes]
    if: needs.detect-changes.outputs.any == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5  # v4
      - uses: sonarmd/workflows/actions/setup-node@main
        with:
          node-version: '18'
      - run: yarn build-shared
      - uses: actions/cache/save@0057852bfaa89a56745cba8c7296529d2fc39830  # v4
        with:
          path: .
          key: workspace-${{ github.sha }}

  test-shared:
    needs: [deps, detect-changes]
    if: needs.detect-changes.outputs.shared == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830  # v4
        with:
          path: .
          key: workspace-${{ github.sha }}
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: '18'
      - run: cd shared && yarn test

  test-admin:
    needs: [deps, detect-changes]
    if: needs.detect-changes.outputs.admin == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830  # v4
        with:
          path: .
          key: workspace-${{ github.sha }}
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: '18'
      - run: cd admin && yarn test
      - run: cd admin && yarn cypress

  test-patient:
    needs: [deps, detect-changes]
    if: needs.detect-changes.outputs.patient == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830  # v4
        with:
          path: .
          key: workspace-${{ github.sha }}
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: '18'
      - run: cd patient && yarn test
      - run: cd patient && yarn cypress

  test-provider:
    needs: [deps, detect-changes]
    if: needs.detect-changes.outputs.provider == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830  # v4
        with:
          path: .
          key: workspace-${{ github.sha }}
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: '18'
      - run: cd provider && yarn test
      - run: cd provider && yarn cypress

  test-seat:
    needs: [deps, detect-changes]
    if: needs.detect-changes.outputs.seat == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830  # v4
        with:
          path: .
          key: workspace-${{ github.sha }}
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: '18'
      - run: cd seat && yarn test
      - run: cd seat && yarn cypress

  lint:
    needs: [deps, detect-changes]
    if: needs.detect-changes.outputs.any == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/cache/restore@0057852bfaa89a56745cba8c7296529d2fc39830  # v4
        with:
          path: .
          key: workspace-${{ github.sha }}
      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020  # v4
        with:
          node-version: '18'
      - run: yarn lint

  bypass:
    needs: [detect-changes]
    if: needs.detect-changes.outputs.any != 'true'
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - run: echo "No changes detected in the frontend codebase. Skipping CI."
