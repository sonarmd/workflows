name            = "smd-signing-keys"
main            = "index.ts"
compatibility_date = "2024-01-01"
account_id      = "0c4f6179a73079dd4847a690bffed296"

# KV namespace binding.
# One-time setup: create the namespace, then paste the id here.
#   wrangler kv namespace create SIGNING_KEYS
# OR via CF dashboard: Workers & Pages → KV → Create namespace "smd-signing-keys"
[[kv_namespaces]]
binding  = "SIGNING_KEYS"
id       = "REPLACE_WITH_KV_NAMESPACE_ID"

[observability]
enabled = true
